rules_version = '2';

// TaskBuddy uses Cloudinary free upload for Notes and Previous Question.
// Firebase Storage is intentionally locked so the project does not require Storage billing/upgrade.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
